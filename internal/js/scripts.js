$(document).ready(function() {
    $(window).bind('resize',function() {
        $('.popover').css('height',$('.popover').width()/3.15);
    });
    $('.popover').css('height',$('.popover').width()/3.15);
});